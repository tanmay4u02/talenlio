import React from "react";
import "./styles.css";

const Wrap = ({ childre, variant, sx }) => {
  return (
    <div className="wrap title" style={sx}>
      Wrap
    </div>
  );
};

export default Wrap;
